{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "windows": {
                "command": "./build.cmd",
                "args": ["${fileDirnameBasename}"]
            },
            "linux": {
                "command": "./build.sh",
                "args": ["${fileDirnameBasename}"]
            },
            "group": "build",
            "problemMatcher": ["$gcc"],
            "detail": "CMake build task for the current lab"
        }
    ]
}
